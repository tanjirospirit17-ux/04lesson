# Домашнее задание №9: Тестирование БД с SQLAlchemy

## Что сделано
- Создана ветка `lesson9` и папка `lesson_09`.
- Написаны 3 автотеста для сущности `students` (добавление, изменение, удаление).
- Использованы библиотеки: `pytest`, `sqlalchemy`, `psycopg2-binary`.
- Тесты стабильны и изолированы: каждый тест создает уникальные данные и гарантированно удаляет их после выполнения (через teardown в фикстурах или явный вызов `.delete()`).
- Код проверен линтером `flake8` и соответствует PEP8.
- Учетные данные БД вынесены в `.env` и добавлены в `.gitignore`.

## Инструкция для наставника по запуску

1. Убедитесь, что установлены зависимости:
   ```bash
   pip install pytest sqlalchemy psycopg2-binary python-dotenv flake8