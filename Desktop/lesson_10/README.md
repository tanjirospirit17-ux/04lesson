# Домашнее задание №10: Интеграция Allure и документирование кода

## Что сделано
- Решение ДЗ №7 (Page Object) перенесено в папку `lesson_10`.
- Весь код задокументирован: добавлены Docstrings для классов и методов.
- Добавлена строгая типизация (type hints) для всех параметров (кроме `self`) и возвращаемых значений.
- Внедрена библиотека `allure-pytest`.
- Тесты и шаги размечены декораторами `@allure.step`, `with allure.step`, `@allure.title`, `@allure.description`, `@allure.feature`, `@allure.severity`.
- Код проверен линтером `flake8` и соответствует PEP8.
- Папки с результатами Allure добавлены в `.gitignore`.

## Инструкция по запуску и формированию отчета

### 1. Установка зависимостей
Убедитесь, что установлены необходимые библиотеки:
```bash
pip install pytest allure-pytest selenium flake8